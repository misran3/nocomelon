[project]
name = "nocomelon-backend"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = []

[tool.uv.workspace]
members = [
    "packages/app",
    "packages/dev",
]
